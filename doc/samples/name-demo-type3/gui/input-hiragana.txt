###
### 名前入力画面(ひらがな)のGUI定義ファイル（Customized by MaiMia MaiMai 2023）
###
### 名前変数 %a に名前を入力できます。
###

# このファイルの基本設定
global {
    # 下記の画像を使用する
    idle:   input-hiragana-idle.png;
    hover:  input-hiragana-hover.png;
    active: input-hiragana-hover.png;
}

# 名前変数のプレビュー領域
NAME_PREVIEW {
    # 種類:名前変数のプレビュー
    type: namevar;

    # 名前変数名
    namevar: a;

    # 座標とサイズ
    x: 520;
    y: 50;
    width: 400;
    height: 60;
}

# 'あ'のボタン
BUTTON_A {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: あ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 87;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'い'のボタン
BUTTON_I {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: い;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 87;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'う'のボタン
BUTTON_U {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: う;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 87;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'え'のボタン
BUTTON_E {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: え;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 87;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'お'のボタン
BUTTON_O {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: お;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 87;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'か'のボタン
BUTTON_KA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: か;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 146;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'き'のボタン
BUTTON_KI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: き;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 146;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'く'のボタン
BUTTON_KU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: く;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 146;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'け'のボタン
BUTTON_KE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: け;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 146;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'こ'のボタン
BUTTON_KO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: こ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 146;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'さ'のボタン
BUTTON_SA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: さ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 203;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'し'のボタン
BUTTON_SI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: し;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 203;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'す'のボタン
BUTTON_SU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: す;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 203;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'せ'のボタン
BUTTON_SE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: せ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 203;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'そ'のボタン
BUTTON_SO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: そ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 203;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'た'のボタン
BUTTON_TA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: た;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 262;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ち'のボタン
BUTTON_TI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ち;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 262;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'つ'のボタン
BUTTON_TU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: つ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 262;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'て'のボタン
BUTTON_TE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: て;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 262;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'と'のボタン
BUTTON_TO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: と;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 262;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'な'のボタン
BUTTON_NA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: な;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 323;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'に'のボタン
BUTTON_NI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: に;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 323;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぬ'のボタン
BUTTON_NU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぬ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 323;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ね'のボタン
BUTTON_NE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ね;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 323;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'の'のボタン
BUTTON_NO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: の;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 323;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'は'のボタン
BUTTON_HA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: は;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 379;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ひ'のボタン
BUTTON_HI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ひ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 379;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ふ'のボタン
BUTTON_HU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ふ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 379;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'へ'のボタン
BUTTON_HE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: へ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 379;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ほ'のボタン
BUTTON_HO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ほ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 379;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ま'のボタン
BUTTON_MA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ま;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 440;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'み'のボタン
BUTTON_MI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: み;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 440;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'む'のボタン
BUTTON_MU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: む;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 440;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'め'のボタン
BUTTON_ME {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: め;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 440;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'も'のボタン
BUTTON_MO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: も;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 440;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'や'のボタン
BUTTON_YA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: や;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 498;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ゆ'のボタン
BUTTON_YU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ゆ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 498;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'よ'のボタン
BUTTON_YO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: よ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 498;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ら'のボタン
BUTTON_RA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ら;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 560;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'り'のボタン
BUTTON_RI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: り;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 560;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'る'のボタン
BUTTON_RU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: る;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 560;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'れ'のボタン
BUTTON_RE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: れ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 560;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ろ'のボタン
BUTTON_RO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ろ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 560;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'わ'のボタン
BUTTON_WA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: わ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 616;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ゐ'のボタン
BUTTON_WI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ゐ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 616;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ゑ'のボタン
BUTTON_WE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ゑ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 616;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'を'のボタン
BUTTON_WO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: を;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 616;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ん'のボタン
BUTTON_N {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ん;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 616;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'が'のボタン
BUTTON_GA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: が;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 732;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぎ'のボタン
BUTTON_GI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぎ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 732;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぐ'のボタン
BUTTON_GU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぐ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 732;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'げ'のボタン
BUTTON_GE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: げ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 732;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ご'のボタン
BUTTON_GO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ご;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 732;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ざ'のボタン
BUTTON_ZA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ざ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 794;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'じ'のボタン
BUTTON_ZI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: じ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 794;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ず'のボタン
BUTTON_ZU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ず;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 794;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぜ'のボタン
BUTTON_ZE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぜ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 794;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぞ'のボタン
BUTTON_ZO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぞ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 794;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'だ'のボタン
BUTTON_DA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: だ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 852;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぢ'のボタン
BUTTON_DI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぢ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 852;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'づ'のボタン
BUTTON_DU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: づ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 852;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'で'のボタン
BUTTON_DE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: で;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 852;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ど'のボタン
BUTTON_DO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ど;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 852;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ば'のボタン
BUTTON_BA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ば;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 911;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'び'のボタン
BUTTON_BI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: び;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 911;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぶ'のボタン
BUTTON_BU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぶ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 911;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'べ'のボタン
BUTTON_BE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: べ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 911;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぼ'のボタン
BUTTON_BO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぼ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 911;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぱ'のボタン
BUTTON_PA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぱ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 970;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぴ'のボタン
BUTTON_PI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぴ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 970;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぷ'のボタン
BUTTON_PU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぷ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 970;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぺ'のボタン
BUTTON_PE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぺ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 970;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぽ'のボタン
BUTTON_PO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぽ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 970;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ゃ'のボタン
BUTTON_XYA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ゃ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1034;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ゅ'のボタン
BUTTON_XYU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ゅ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1034;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ょ'のボタン
BUTTON_XYO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ょ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1034;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'っ'のボタン
BUTTON_XTU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: っ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1034;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ゎ'のボタン
BUTTON_XWA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ゎ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1034;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぁ'のボタン
BUTTON_XA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぁ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1091;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぃ'のボタン
BUTTON_XI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぃ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1091;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぅ'のボタン
BUTTON_PU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぅ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1091;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぇ'のボタン
BUTTON_XE {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぇ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1091;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ぉ'のボタン
BUTTON_XO {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ぉ;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1091;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 'ー'のボタン
BUTTON_BOU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ー;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1148;
    y: 170;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# '。'のボタン
BUTTON_MARU {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: 。;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1148;
    y: 259;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# '、'のボタン
BUTTON_TEN {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: 、;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1148;
    y: 349;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# '！'のボタン
BUTTON_BIKKURI {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ！;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1148;
    y: 443;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# '？'のボタン
BUTTON_HATENA {
    # 種類:名前変数の入力
    type: char;

    # 名前変数名
    namevar: a;

    # 入力する文字列
    msg: ？;

    # 最大文字数
    max: 6;

    # 座標とサイズ
    x: 1148;
    y: 532;
    width: 53;
    height: 57;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 決定ボタン
BUTTON_OK {
    # 種類:名前変数の入力
    type: char;

    # 入力する文字列
    msg: "[ok]";

    # 座標とサイズ
    x: 1109;
    y: 637;
    width: 105;
    height: 55;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# クリアボタン
BUTTON_CLEAR {
    # 種類:名前変数の入力
    type: char;

    # 入力する文字列
    msg: "[clear]";

    # 座標とサイズ
    x: 890;
    y: 637;
    width: 140;
    height: 55;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# カタカナボタン
BUTTON_KATAKANA {
    # 種類:他のGUIへの移動
    type: gui;

    # 移動先のGUIファイル
    file: input-katakana.txt;

    # 座標とサイズ
    x: 300;
    y: 621;
    width: 236;
    height: 94;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

# 一文字消去ボタン
BUTTON_BACKSPACE {
    # 種類:
    type: char;

    # 入力する文字列
    msg: "[backspace]";

    # 座標とサイズ
    x: 588;
    y: 637;
    width: 225;
    height: 55;

    # ポイントされたときの効果音
    pointse: btn-change.ogg;

    # クリックされたときの効果音
    clickse: click.ogg;
}

